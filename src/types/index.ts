/**
 * Types shared across the Phase 1 shell. Feature-specific types live with their
 * feature folder (`src/features/*`) — these are the cross-cutting ones.
 */

export type { NavItem } from "./navigation";
export type { NavIconName } from "./navigation";
